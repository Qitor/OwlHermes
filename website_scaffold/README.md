# Website Scaffold v2

MVP pages:

1. `/` latest digest and top signals.
2. `/signals` signal list with filters.
3. `/signals/[id]` detail page.
4. `/sources` source health.
5. `/podcasts` podcast episodes and transcript status (Round 2).
6. `/events` monitored events and agenda/material changes (Round 2).
7. `/benchmarks` benchmark registry and observations (Round 3).

Keep the first version simple. The value is signal quality, not visual complexity.
